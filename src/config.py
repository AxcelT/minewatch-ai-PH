# Configuration file (API keys, file paths, etc.)

# Replace 'your-api-key' with your actual GPT API key.
OPENAI_API_KEY = 'your-api-key'

# Additional configuration (frame extraction interval, file paths, etc.)
FRAME_INTERVAL = 30  # Adjust this number as needed
VIDEO_PATH = 'data/sample_video.mp4'